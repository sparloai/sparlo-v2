import { TablePageSkeleton } from '~/components/skeletons';

/**
 * Admin page loading skeleton.
 * Uses TablePageSkeleton to match admin data table layout.
 */
export default function AdminLoading() {
  return <TablePageSkeleton />;
}
