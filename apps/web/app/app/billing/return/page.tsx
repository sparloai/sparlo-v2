// We reuse the page from the billing module
// as there is no need to create a new one.
import ReturnCheckoutSessionPage from '~/app/[account]/billing/return/page';

export default ReturnCheckoutSessionPage;
